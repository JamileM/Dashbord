<script>
async function carregarDados() {
    const response = await fetch('dados.csv');
    const data = await response.text();

    const linhas = data.split('\n').slice(1);

    let total = 0;
    let ativas = 0;
    let atrasadas = 0;

    let statusCount = {};
    let devCount = {};

    const hoje = new Date();

    linhas.forEach(linha => {
        const col = linha.split(',');

        const desenvolvedor = col[2];
        const status = col[5];
        const dataEntrega = new Date(col[7]);

        total++;

        // status
        statusCount[status] = (statusCount[status] || 0) + 1;

        // dev
        devCount[desenvolvedor] = (devCount[desenvolvedor] || 0) + 1;

        // ativas
        if (status === "Em Andamento") {
            ativas++;
        }

        // atraso
        if (dataEntrega < hoje && status !== "Concluído") {
            atrasadas++;
        }
    });

    document.getElementById('total').innerText = total;
    document.getElementById('ativas').innerText = ativas;
    document.getElementById('atrasadas').innerText = atrasadas;

    let percentual = ((atrasadas / total) * 100).toFixed(1);
    document.getElementById('percent').innerText = percentual + "%";

    // gráfico status
    new Chart(document.getElementById('statusChart'), {
        type: 'bar',
        data: {
            labels: Object.keys(statusCount),
            datasets: [{
                data: Object.values(statusCount),
                backgroundColor: ['#28a745','#007bff','#ffc107','#dc3545']
            }]
        }
    });

    // gráfico dev
    new Chart(document.getElementById('devChart'), {
        type: 'bar',
        data: {
            labels: Object.keys(devCount).slice(0,10),
            datasets: [{
                data: Object.values(devCount).slice(0,10),
                backgroundColor: '#007bff'
            }]
        },
        options: { indexAxis: 'y' }
    });
}

carregarDados();
</script>